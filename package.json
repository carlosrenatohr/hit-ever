{
  "name": "hit-ever2",
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "test": "vitest run",
    "check": "vitest run && wrangler deploy --dry-run --outdir dist",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.12.7",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "vitest": "^4.1.8",
    "wrangler": "^4.4.0"
  },
  "version": "1.0.0"
}
