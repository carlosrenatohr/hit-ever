{
  "name": "hit-ever2",
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "test": "vitest run",
    "check": "vitest run && wrangler deploy --dry-run --outdir dist",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "import:billing": "tsx src/modules/billing/ingest/import-xlsx.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.12.7",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "exceljs": "^4.4.0",
    "lefthook": "^1.12.1",
    "tsx": "^4.19.2",
    "vitest": "^4.1.8",
    "wrangler": "^4.4.0"
  },
  "version": "1.0.0"
}
